# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. **Task 1 — <name>**: <what changes>. _Verify: <how>._
2. **Task 2 — <name>**: ...
3. ...

## Task 1 context brief
<paste the full brief you'll give Claude>

## Task 1 — what happened
- Claude's plan summary:
- What I pushed back on:
- Verification result:

## Task 2 — what happened
- ...

## Task 3 — what happened
- ...

## Reflection — where I redirected
- (3 sentences)

## Decomposition (6 tasks)

1. **Task 1 — Add tags key to each note dict**: Store a list of strings in a tags key. _Verify: New notes will have an empty tags list if no tags are given._

2. **Task 2 — Add tags input to note creation form**: Allow users to enter tags when creating a note. _Verify: New tags are added when inputted._

3. **Task 3 — Clean tag input**: Remove whitespace and ignore empty tags. _Verify: Input with spaces becomes normal and empty tags are removed._

4. **Task 4 — Display tags on notes list**: Show tags alongside notes. _Verify: Notes without tags and notes with tags can both be seen correctly._

5. **Task 5 — Add tag filtering**: Allow filtering notes by tag. _Verify: Notes can be filtered using a selected tag._

6. **Task 6 — Add tests**: Create tests for tag parsing and filtering. _Verify: Old and new tests all pass._


## Task 1 context brief

Goal: Add tags key to each note dictionary in the sandbox notes app, with each tags key storing a list of strings and defaulting to an empty list when a note is made with no tags.

Constraints:
- No new models or schemas are added.
- Don't modify existing note fields.
- Make sure existing tests pass.
- An in-memory list of dictionaries are used, not a database.

Files That Should Be Changed:
- app.py
- Any tests that needed to be updated for the newly created default field.

Acceptance:
- Tags keys are added to new notes that are created, with new notes that don't get created with tags have an empty list for tags.
- All existing functionalities work and tests pass.

Make a plan but don't write code.

