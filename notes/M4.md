# M4 — Reading and Verifying Output

## Bug hunt #1 — SQL injection
- Bug:
- Why Claude wrote it:
- Fix:

## Bug hunt #2 — hallucinated package
- Bug:
- Why Claude wrote it:
- Fix:

## Bug hunt #3 — edge case
- Bug:
- Why Claude wrote it:
- Fix:

## Bug hunt #4 — hardcoded secret
- Bug:
- Why Claude wrote it:
- Fix:

## Test I wrote
- Bug I picked:
- Test code:
- Confirmed it fails on the bug and passes on the fix? (yes/no)

## Near-miss reflection
- (3-5 sentences)

## Bug hunt #1 — SQL injection
- Bug: User input is put directly into the SQL due to an f-string, so it could face SQL injection attacks.
- Why Claude wrote it: Even though it is weak, it produces the right output/passes tests.
- Fix: Don't use string interpolation so the user input becomes a piece of data instead.


## Bug hunt #2 — hallucinated package
- Bug: The flask_simpleauth package isn't real.
- Why Claude wrote it: Sometimes Claude will make up packages because it's AI.
- Fix: Use a different import/package that actually exists and is documented.


## Bug hunt #3 — edge case
- Bug: It wouldn't work if ratings was an empty list because you can't divide by 0.
- Why Claude wrote it: Claude probably forgot to account for this case where the ratings list is empty.
- Fix: The code should account for the edge case where the ratings list is empty before calculating the average.


## Bug hunt #4 — hardcoded secret
- Bug: It shows the OpenAI API key, which should be hidden as an environment variable.
- Why Claude wrote it: Claude didn't realize it should store the API key in an environment variable instead of hardcoding it.
- Fix: Move the API key into an environment variable and load it from there rather than storing it directly in the code.


## Test I wrote
- Bug I picked: #3

- Test code:

```python
def check_not_zero_division():
    assert average_rating([]) is None

The test won't pass for the current code because it doesn't account for the average_rating list being empty, but the fixed code will pass.
